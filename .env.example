# MiniMax 主模型和 Qwen3 Embedding 使用同一套余额。
# 接口地址、模型名、OpenAI 兼容模式和 embedding 模型名均已在代码里固定默认值。
MINIMAX_API_KEY=replace-with-real-key

# 秘塔搜索。只填密钥即可；接口地址使用代码默认值。
METASO_API_KEY=

# Metaculus 直连题池。未配置时 LiveBench 仍会运行，但题源健康会标记 degraded。
METACULUS_API_TOKEN=
